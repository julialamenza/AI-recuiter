# Omit or leave empty in dev: requests use /api via the Vite proxy (see vite.config.ts).
# Set this when the UI and API are on different hosts (e.g. production).
# VITE_API_BASE_URL=http://localhost:8000
